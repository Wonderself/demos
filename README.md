# Demos — WonderSelf

Hiring portfolio. Not a raise.

Live (once Pages is on): https://wonderself.github.io/demos/

- EN (default): `index.html`
- FR: `fr.html`
- HE: `he.html`
- Method: `method.html` (`?l=fr` / `?l=he`)

Domains live in `app.js` → `LINKS`.

## Status 2026-09-07

- Repo public, files ready.
- `cinegeny.com` resolves and serves (Next.js).
- `*.flash-site.pro` has **no DNS** yet — cards will 404 until you point the zone.
- First Pages deploy needs the site created. Workflow now uses `configure-pages` with `enablement: true`.
  If Actions still cannot create it, one click:
  Settings → Pages → Source = **GitHub Actions** → Save.
